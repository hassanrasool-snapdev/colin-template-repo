# Database (PostgreSQL)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Session Management
SESSION_SECRET="your-session-secret-here"

# Firebase Configuration (for authentication and storage)
GOOGLE_APPLICATION_CREDENTIALS="path/to/firebase-admin-key.json"
# Alternative to GOOGLE_APPLICATION_CREDENTIALS - use either this OR the file path above
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'
VITE_FIREBASE_PROJECT_ID="your-firebase-project-id"
FIREBASE_STORAGE_BUCKET="your-project-id.firebasestorage.app"

# Stripe (for payments)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
STRIPE_PRICE_ID_PRO="price_test_pro_subscription_id"

# SendGrid (for emails)
SENDGRID_API_KEY="SG...."

# PostHog (for analytics)
POSTHOG_API_KEY="phc_..."
POSTHOG_HOST="https://app.posthog.com"

# OpenAI (for AI chat functionality)
OPENAI_API_KEY="sk-your-openai-api-key-here"
# Optional AI configuration
# Optional: Select models per tier
# Use modern, available models
AI_MODEL_FREE="gpt-4o-mini"
AI_MODEL_PRO="gpt-4o"
# Fallback single model (used if tiered vars not set)
OPENAI_MODEL="gpt-4o-mini"
AI_MAX_TOKENS=1024
AI_TEMPERATURE=0.7
AI_TOP_P=1
AI_SYSTEM_PROMPT="You are a helpful, concise assistant. Avoid unsafe or sensitive outputs."
AI_RATE_LIMIT_WINDOW_MS=60000
AI_RATE_LIMIT_MAX=20
# Number of tool-call steps the AI runtime can take (formerly AI_MAX_TOOL_ROUNDTRIPS)
AI_MAX_STEPS=3

# Frontend/Deployment Configuration
FRONTEND_URL="http://localhost:5000"
